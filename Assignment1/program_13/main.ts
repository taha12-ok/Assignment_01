const favoritetransportation: string[] = ["Tesla Model S", "Honda CBR600RR", "BMW M3", "Harley-Davidson Fat Boy"];
for (const item of favoritetransportation) {
    console.log(`I would like to own a ${item}.`);
}